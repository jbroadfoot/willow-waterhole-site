WWGC build v5.6
2026-08-12
Events + Anniversary refinements:

ANNIVERSARY (anniversary.html):
- Early-photo gallery was 5 images in one row (repeat(5,1fr)) — too small to see. Now 6 images in
  2 rows of 3 (repeat(3,1fr)), each ~66% larger. Added "Music in the Park (2011)" as the 6th image
  (new file images/anniversary/music-in-the-park-2011.jpg, optimized from 1.6MB/3624px to 146KB/1200px).

EVENTS (events.html):
- Featured area now shows TWO cards side by side: Colors of the Prairie (flagship) + Family Fishing
  Clinic (was buried in the list). New .ev-feature-grid (2-col, stacks on mobile). Was one wide card.
- Added small muted category tags to every event in the list (Birding / Volunteer / Family / Festival /
  Fundraiser Walk / Community) — text pills, one subtle tint each, no busy rainbow. New .ev-tag CSS.
- Removed the .ev-row:hover slide/border movement (desktop-only, didn't aid comprehension).

IMAGE REDUNDANCY (audit finding, informational):
- donald-perkins-trail-open.jpg was the most-reused photo (events hero + donate + getinvolved + news x2 +
  its article). Events hero left unchanged for now per your call (no current library image fits events).
- Lakes photos reappearing on Lakes & Trails + Things to Do is expected (same subjects, different framing).

FLAGS (need your eyes — can't verify image contents from here):
- Family Fishing card uses images/activities/pier2.jpg (picked by filename). Swap if it's not a fishing shot.
- Family Fishing card links to Eventbrite (no article exists); Colors links to its news article. Cards click differently.

Build comments bumped v5.6 on anniversary.html + events.html. Cache-bust ?v=5.6 across all 20 pages.
Still open from earlier: Events hero photo (deferred — no fitting library image).
